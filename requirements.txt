# 베어링 RUL 예측 시스템 필수 패키지
# PHM 2025 Challenge - Bearing Remaining Useful Life Prediction

# ========================================
# 핵심 필수 패키지 (반드시 설치 필요)
# ========================================
numpy>=1.21.0          # 수치 계산, 배열 처리
pandas>=1.3.0          # 데이터프레임, CSV/Excel 처리
scipy>=1.7.0           # 신호 처리 (butter, filtfilt, iirnotch)
scikit-learn>=1.0.0    # PCA, MinMaxScaler, 머신러닝
torch>=1.9.0           # 딥러닝, Transformer 모델
nptdms>=1.4.0          # TDMS 파일 읽기 (베어링 데이터)
tqdm>=4.62.0           # 진행률 표시
openpyxl>=3.0.0        # Excel 파일 생성 (제출 파일)

# ========================================
# 추가 권장 패키지
# ========================================
matplotlib>=3.5.0      # 시각화, 디버깅용
seaborn>=0.11.0        # 고급 시각화
xlsxwriter>=3.0.0      # Excel 파일 쓰기 대안

# ========================================
# 설치 가이드
# ========================================
# 1. 기본 설치:
#    pip install -r requirements.txt
#
# 2. GPU 지원 PyTorch (CUDA 11.8):
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# 3. CPU 전용 PyTorch:
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
